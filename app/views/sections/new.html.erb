<h1 class="in-form-title">Create new section</h1>
<%= form_for(@section, :html => {:class => 'in-form'}) do |f| %>
	<div class="in-row">
		<%= f.text_field :name, class: "in-field", id: "section-title" %>
		<label class="in-label" for="section-title">
	      <span class="in-label-content" data-content="Title">Title</span>
	    </label>
	</div>
	<!-- div class="form-group">
		<%= label_tag 'section[category_id]', "Category" %>
		<%= f.collection_select(:category_id, Category.all, :id, :name, {prompt: true}, {class: "form-control"}) %>
	</div -->
	<%= f.submit "Submit", :name => "cat[#{@category.id}]", class: "btn btn-primary in-submit" %>
<% end %>

