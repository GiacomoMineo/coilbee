
<h1>Search Results:<h1>
<% @tags.each do |tag| %>
  <div class="tag">
    <h2 class="tag-title"><%= link_to tag.name, tag %></h2>
  </div>
<% end %>
<% if @tags.empty? %>
	<h2>No search results!</h2>
<% end %>	